# singapore-transport-AI-agent
Agentic Singapore public transport AI assistant built with a LangGraph-style workflow using LTA DataMall + open APIs. Handles bus/traffic/taxi queries with context (weather, peak-hour, holidays/events), includes deterministic fallback, and demonstrates multi-user simulations with clear, styled outputs.
